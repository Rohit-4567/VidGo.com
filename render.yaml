services:
  - type: web
    name: VidGO
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"   # Yeh sahi hona chahiye
    plan: free
    region: oregon
